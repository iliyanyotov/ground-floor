# Normalize every text file to LF — in the repo and on checkout — so clones
# behave identically on every OS. `text=auto` skips files git detects as
# binary, so no explicit binary list is needed. Catches what a per-extension
# list would miss: Markdown, YAML, TOML, and the shell hooks in .husky/.
* text=auto eol=lf
